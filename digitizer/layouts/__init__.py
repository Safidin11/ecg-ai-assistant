"""Модульная архитектура: по одному независимому пайплайну на формат ЭКГ.

Каждый format_X/ имеет одинаковый интерфейс: pipeline.run(image_path, config)
-> common.types.DigitizeResult. Общие формат-независимые кирпичи — в common/.

Эталонный пайплайн 12×1 (digitizer/stages/) не входит сюда и не изменяется;
format_12_1/ — лишь тонкий адаптер, вызывающий его.
"""
