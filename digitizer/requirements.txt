# Ядро распознавания раскладки (лёгкие зависимости)
numpy>=1.26
pillow>=10.0
rapidfuzz>=3.0

# OCR-движок для реальных картинок (тяжёлый: тянет torch).
# Ставится отдельно, когда нужен реальный прогон, а не только тесты/логика:
#   pip install easyocr
# easyocr>=1.7

# Тесты
pytest>=8.0
